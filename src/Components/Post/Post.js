import Form from "../Form/Form";
import React, { Component } from 'react';

class Post extends Component {
  render() {
    return(
        <div>
          Post
        </div>
    );
  }
}

export default Post;